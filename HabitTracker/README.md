# HabitTracker

This folder contains a SwiftUI UI skeleton for the habit tracker app.

## How to run

1. Create a new iOS App project in Xcode.
2. Copy the Swift files from this folder into your project.
3. Make sure the files are added to the app target.
4. Build and run.

Data is persisted locally using UserDefaults via JSON encoding.
